# Stream integration

This module uses the existing community Discord client. Setup and operating limits: [STREAM-SETUP.md](../STREAM-SETUP.md).

Run offline tests from the repository root: `node --test stream/*.test.js`.
