# Existing Discord application: keep the existing DISCORD_TOKEN and other variables.
# Add these to the existing Railway service. Never commit actual secrets.
TWITCH_CHANNEL=toomturboo
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
STREAM_GUILD_ID=
PUBLIC_URL=https://bot-production-d58c.up.railway.app
# Optional: existing MASTER_USER_IDS may authorize stream management.
# Persistent data uses the existing RAILWAY_VOLUME_MOUNT_PATH=/data.
